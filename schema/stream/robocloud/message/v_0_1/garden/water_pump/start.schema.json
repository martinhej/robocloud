{
  "$schema": "http://robomaze.io/message/v_0_1/garden/water_pump/start/schema#",
  "title": "Start Water Pump",
  "description": "",
  "type": "object",
  "properties": {
    "pumpId": {
      "description": "The Id of the Pump to start.",
      "type": "string"
    },
    "runTime": {
      "description": "The run time in seconds.",
      "type": "int"
    },
    "flowLevel": {
      "description": "The flow level the Pump should through output.",
      "type": "int"
    }
  },
  "required": ["pumpId", "runTime"]
}